<%= form_with(model: e_doc, local: true) do |form| %>
  <% if e_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(e_doc.errors.count, "error") %> prohibited this e_doc from being saved:</h2>

      <ul>
        <% e_doc.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>

  <% end %>

 

  <div class="field">
    <%= form.label :docname %>
    <%= form.text_field :docname %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
